////////////////////////////////////////////////////////////////////////////////////////////////////

// standard libraries
use clap::ValueEnum;
use core::str::FromStr;

////////////////////////////////////////////////////////////////////////////////////////////////////

/// Log flag enum.
#[derive(Clone, Debug, Eq, PartialEq, ValueEnum)]
pub enum LogFlag {
  Info,
  Debug,
  Warn,
  Error,
}

////////////////////////////////////////////////////////////////////////////////////////////////////
